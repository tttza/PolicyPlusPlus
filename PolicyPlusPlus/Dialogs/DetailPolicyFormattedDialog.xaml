<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="PolicyPlusPlus.Dialogs.DetailPolicyFormattedDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Detailed formatted"
    PrimaryButtonText="Copy"
    SecondaryButtonText="Close"
    DefaultButton="Secondary"
    MinWidth="720">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBlock Text="Section:" VerticalAlignment="Center"/>
                <ComboBox x:Name="SectionSelector" Width="140" SelectionChanged="SectionSelector_SelectionChanged">
                    <ComboBoxItem Content="Computer"/>
                    <ComboBoxItem Content="User"/>
                </ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBlock Text="View:" VerticalAlignment="Center"/>
                <ComboBox x:Name="ViewSelector" Width="160" SelectionChanged="ViewSelector_SelectionChanged">
                    <ComboBoxItem Content="Formatted"/>
                    <ComboBoxItem Content=".reg export"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <TextBox x:Name="OutputBox"
                     FontFamily="Consolas"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     IsReadOnly="True"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
        </ScrollViewer>

        <TextBlock Grid.Row="3" x:Name="HintText" Opacity="0.7"/>
    </Grid>
</ContentDialog>
