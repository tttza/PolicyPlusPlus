<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PolicyPlus.WinUI3.Windows.ListEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="{ThemeResource WindowBackground}" Padding="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="HeaderText" Grid.Row="0" FontWeight="SemiBold"/>
    <ListView x:Name="ListItems" Grid.Row="1" SelectionMode="None"/>
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal" Spacing="8">
        <Button x:Name="AddBtn" Content="Add"/>
      </StackPanel>
      <Button x:Name="OkBtn" Grid.Column="1" Content="OK" Style="{StaticResource PrimaryButtonStyle}"/>
      <Button x:Name="CancelBtn" Grid.Column="2" Content="Cancel"/>
    </Grid>
  </Grid>
</Window>
